{
  "name": "hello-api",
  "version": "0.1.0",
  "description": "A secure, offline-first HTTP API testing and management tool",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --write \"**/*.{js,html,css,json,md}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{js,html,css,json,md}\" --ignore-path .gitignore",
    "gen-icons": "node scripts/gen-icons.js",
    "build": "mkdir -p dist && cp -r index.html manifest.json sw.js src assets dist/"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
